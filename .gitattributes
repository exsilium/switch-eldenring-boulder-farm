# Normalize line endings. Shell/Docker/CI files must use LF so they work inside
# the Linux build container and on GitHub Actions, even when authored on Windows.
* text=auto eol=lf

# Windows-native launchers keep CRLF.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
